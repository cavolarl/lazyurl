# LazyURL - A go script to get id:s from booking.com / tripadvisor / hotels.com links

Just paste the link at you will get the id.

```zsh
go run lazyurl.go
```

or

```zsh
go build -o lazyurl && ./lazyurl
```
